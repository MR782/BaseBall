using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Runner : MonoBehaviour
{
    public float Speed = 0;     //スピード
    public float Avoidance = 0; //回避力

    // Update is called once per frame
    void Update()
    {
        
    }
}
